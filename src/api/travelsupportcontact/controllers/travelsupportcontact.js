'use strict';

/**
 *  travelsupportcontact controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::travelsupportcontact.travelsupportcontact');
